import React from "react";
import styled from "styled-components";

function Icon() {
    return(<StyledIcon />)
}

const StyledIcon = styled.div`
    width: 20px;
    height: 20px;
    background: red;
`;

export default Icon;